# staticman (comments) configuration
staticman_url: https://yume-comments.herokuapp.com/v2/entry/harkdand/yume-comments/master/comments

# _config.yml (defaults)
repository  : "harkdand/yume-comments"
comments:
  provider  : "staticman_v2"
  staticman:
    branch    : "master"
    
defaults:
  # Post defaults
    values:
      comments: true
